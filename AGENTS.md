# Agents

## Setup

Python venv at `.venv`. Activate with `source .venv/bin/activate`.

## Conventions

- Engine is Rust. Python is for plotting/scripts only.
- Game is fully deterministic — no randomness anywhere.
- Plan lives in `PLAN.md`.
